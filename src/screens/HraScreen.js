import React from 'react';
import { StyleSheet, Text } from 'react-native';

const HraScreen = () => {
  return (
    <Text style={{fontSize: 24, fontWeight: "bold"}}>Hra Screen</Text>
  );
};

const styles = StyleSheet.create({});

export default HraScreen;
