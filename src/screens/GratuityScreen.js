import React from 'react';
import { StyleSheet, Text } from 'react-native';

const GratuityScreen = () => {
  return (
    <Text style={{fontSize: 24, fontWeight: "bold"}}>Gratuity Screen</Text>
  );
};

const styles = StyleSheet.create({});

export default GratuityScreen;
