import React from 'react';
import { StyleSheet, Text } from 'react-native';

const SalaryScreen = () => {
  return (
    <Text style={{fontSize: 24, fontWeight: "bold", alignSelf: "center"}}>Salary Screen</Text>
  );
};

const styles = StyleSheet.create({});

export default SalaryScreen;
